# bat config — `ansi` emits the 16 standard ANSI colors instead of a fixed
# palette, so output follows the terminal's own colorscheme (Ghostty) with no
# per-theme upkeep. Mirrors the dirvish bat preview, which uses --theme=ansi too.
--theme="ansi"
