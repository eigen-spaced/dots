# Mac OS files
.DS_Store

nvim/.config/nvim/plugin

zsh/.config/zsh/plugins
zsh/.config/zsh/zsh_history
zsh/.config/zsh/.zsh_history
zsh/.config/zsh/.zsh_sessions
zsh/.config/zsh/.zcompdump

.lsp
.clj-kondo

# Doom: machine-local personal values (name/email). Tracked template lives in
# doom/.doom.d/private.el.example.
doom/.doom.d/private.el

# flycheck temp files (created next to a checked file; config.el is symlinked
# into this repo, so they can land here)
flycheck_*.el
